---
description: Bootstrap KEEP in this repo — scaffold /knowledge, install SPEC-000-keep, append the KEEP snippet to AGENTS.md/CLAUDE.md. Run once per repo.
argument-hint: (no args)
---

Input: `$ARGUMENTS`

This is a thin entry point for `/keep-init`. The full, canonical contract lives inside the skill so it travels with both the plugin and skill-only installs:

**`skills/KEEP/references/commands/keep-init.md`**

Read that file and follow it exactly against the current repository. It is the single source of truth for this command — make any behavior change there, never here.
