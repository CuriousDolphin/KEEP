---
description: Capture a half-formed idea durably in /knowledge/ideas/ — not yet a spec or ADR
argument-hint: <idea description, free-form>
---

Input: `$ARGUMENTS`

This is a thin entry point for `/keep-idea`. The full, canonical contract lives inside the skill so it travels with both the plugin and skill-only installs:

**`skills/KEEP/references/commands/keep-idea.md`**

Read that file and follow it exactly against the current repository. It is the single source of truth for this command — make any behavior change there, never here.
