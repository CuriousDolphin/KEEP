---
description: KEEP root command — shows /knowledge status as a dashboard, or points to /keep-init if missing. Run with no args.
argument-hint: (no args)
---

Input: `$ARGUMENTS`

This is a thin entry point for `/keep`. The full, canonical contract lives inside the skill so it travels with both the plugin and skill-only installs:

**`skills/KEEP/references/commands/keep.md`**

Read that file and follow it exactly against the current repository. It is the single source of truth for this command — make any behavior change there, never here.
